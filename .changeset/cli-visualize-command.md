---
"@topojs/cli": minor
---

Add `visualize` command with interactive canvas-based graph visualizer served over HTTP. Supports `--port` option (default 7331). Renders nodes and edges with force-directed layout, pan/zoom, and per-edge-kind color coding.
