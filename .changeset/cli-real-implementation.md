---
"@topojs/cli": minor
---

Implement real CLI commands — analyze, check, trace, optimize, and export now load actual statespace files (compiled JS/MJS) and introspect the graph. The `run` function is now async. Adds `--format json|dot|mermaid` support for `export`.
